name: Weather GEFS Local scheduler

on:
  push:
    paths:
      - ".github/workflows/weather-gefs-local-scheduler.yml"
  schedule:
    - cron: "40 2,8,14,20 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: weather-gefs-local-clock
  cancel-in-progress: true

jobs:
  ensemble:
    uses: kenzuko/Jotrip-Lab/.github/workflows/weather-gefs-local-matrix.yml@feat/weather-lab-data-engine-v1
    with:
      target_ref: feat/weather-lab-data-engine-v1
      smoke: false
    secrets: inherit
# full matrix trigger
# 03:40 / 09:40 / 15:40 / 21:40 UTC+7 - leaves margin before 06:00/18:00 MASTER reports

# island-watch refresh

# trigger-10d-full-gefs

# trigger-local-exposure-v1

# prod5-gefs-exposure-retry

# weather-v5-spatial-production-trigger

# trigger-calibration-analytics-v1

# trigger-calibration-analytics-v2

# trigger-full-horizon-cycle-v1

# restore-island-only-gefs-after-rg-feed-lock

# manual-cycle-resync-20260922-1720
