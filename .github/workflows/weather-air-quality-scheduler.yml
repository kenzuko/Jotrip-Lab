name: Weather Air Quality scheduler

on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  air-quality:
    uses: kenzuko/Jotrip-Lab/.github/workflows/weather-air-quality.yml@feat/weather-lab-data-engine-v1
    with:
      target_ref: feat/weather-lab-data-engine-v1
    secrets: inherit
